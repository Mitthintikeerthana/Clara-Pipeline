# Clara Pipeline - Python dependencies
# Install: pip install -r requirements.txt
#
# All packages are free and open-source.
# Core pipeline runs on Python 3.10+

# -- LLM (Google Gemini - free tier) ------------------------------------------
# New SDK (replaces deprecated google-generativeai)
google-genai>=1.0.0

# -- .env auto-loading ---------------------------------------------------------
python-dotenv>=1.0.0

# -- Local transcription (optional - only needed for audio input) --------------
# openai-whisper runs 100% locally, no API calls or cost.
# Install separately if you plan to transcribe audio files:
#   pip install openai-whisper
# Already installed on this machine.
openai-whisper>=20231117
